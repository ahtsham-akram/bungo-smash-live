const toggle=document.querySelector('.nav-toggle');const links=document.querySelector('.nav-links');if(toggle&&links)toggle.addEventListener('click',()=>links.classList.toggle('open'));
document.querySelectorAll('.tab').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.menu-section').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.getElementById(btn.dataset.target).classList.add('active')}));
const form=document.querySelector('#reservationForm');if(form)form.addEventListener('submit',e=>{e.preventDefault();const status=document.querySelector('#formStatus');status.textContent='Thanks — your reservation request is ready to be connected to email, Formspree, or a backend later.';status.classList.add('flash');form.reset();setTimeout(()=>status.classList.remove('flash'),1300)});
